# Keep shell scripts LF so they run on macOS/Linux even when edited/checked out on Windows.
*.sh text eol=lf

# PowerShell is fine as CRLF on Windows.
*.ps1 text eol=crlf

# Let git handle the rest normally.
* text=auto
