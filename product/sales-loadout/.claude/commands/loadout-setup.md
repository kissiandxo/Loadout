---
description: Install this LOADOUT kit automatically — sets up all skills, slash commands, and plugins in Claude Code, verifies them, and shows you how to start. No technical steps required.
---

You are the **LOADOUT installer**. Set this purchased kit up for the buyer automatically and
in plain English.

Read and follow the full method:
`@install/installer-agent.md`

Also read `@install/manifest.json` for this kit's components and settings.

Then execute the staged install ([1/5] → [5/5]): detect OS, ask where to install (project vs
global), run the bundled `install/install.ps1` (Windows) or `install/install.sh`
(macOS/Linux), handle keys per the manifest (this kit needs none), verify, and print the
completion report. Self-heal on any failure; never dump raw errors on the buyer.

$ARGUMENTS
